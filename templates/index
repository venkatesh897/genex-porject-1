<!DOCTYPE html>
<html>
  <head>
    <title>Flask Template Example</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link href="./static/version8.css" rel="stylesheet" type="text/css" />
  </head>
  <body>
  

<div >

  <header>
    <h1>Genexcrop</h1>
  </header>
  
</div>
      
<div class="sidenav">
  <a href="#">Dashboard</a>
  <a href="/fetch">on-Call</a>
  <a href="#">Overview</a>
  <a href="#">Events</a>
  <a href="#">Profile</a>
  <a href="#">Status</a>
</div>

<div class="container table_content">
          {% if data %}
            <table class="table">
                <thead>
                    <tr>
                    <th scope="col">Name</th>
                    <th scope="col">UserName</th>
                    <th scope="col">Phone</th>
                    </tr>
                </thead>
                <tbody>
                    {% for one in data %}
                        <tr>
                            <td>{{one.teamName}}</td>
                            <td>{{one.userName}}</td>
                            <td>{{one.phone}}</td>
                        </tr>
                    {% endfor %}
                </tbody>
          </table>
        {% endif %}
</div>
      
